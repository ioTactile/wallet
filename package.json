{
  "name": "wallet",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint": "pnpm --dir mobile lint && pnpm --dir api lint && pnpm --dir shared lint",
    "format": "pnpm --dir mobile format && pnpm --dir api format && pnpm --dir shared format",
    "format:check": "pnpm --dir mobile format:check && pnpm --dir api format:check && pnpm --dir shared format:check"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  }
}
